{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Compiled C Program",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            // "preLaunchTask": "Compile C program" // Раскомментируйте, если нужна компиляция
        }
    ]
}